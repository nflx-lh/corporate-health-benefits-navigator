# Architecture (MVP)

- Frontend (React/Vite)
- Backend API (FastAPI)
- Agent Orchestration (LangGraph, planned)
- Retrieval + Rules Engine (planned)
- Data: policy docs + rules + fixtures

## Initial Flow
UI -> /v1/query -> orchestrator (stub) -> response
